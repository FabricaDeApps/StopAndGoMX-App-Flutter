import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

/// Placeholder temporal para `celtas`.
///
/// Reemplaza este archivo con la salida real de:
/// `flutterfire configure --android-app-id=app.stopandgomx.celtas --ios-bundle-id=app.stopandgomx.celtas --platforms=android,ios --out=lib/firebase_options_celtas.dart`
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase para el flavor celtas no ha sido configurado todavia. '
      'Genera lib/firebase_options_celtas.dart con FlutterFire.',
    );
  }
}
